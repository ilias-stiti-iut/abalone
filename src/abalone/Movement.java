package abalone;

/**
 * Player's movement possible
 * this class will enable to make movements (Ball.setNewPos and Position.setPos may be useless now)
 * @author stitii
 *
 */
public class Movement
{
	private static final int MAX_LINEMOV = 9;
	
	/**
	 * table which store the line of balls which will be moved
	 */
	private Ball[] lineMov;
	
	//private boolean movPossible();
	
	//private void mov(param?);
}
